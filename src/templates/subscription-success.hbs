<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <title>Thông báo thanh toán thành công</title>
    <style>
      body {
        font-family: Arial, Helvetica, sans-serif;
        background-color: #f5f5f5;
        padding: 20px;
      }
      .container {
        max-width: 480px;
        margin: auto;
        background: #ffffff;
        border-radius: 8px;
        padding: 24px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
      }
      .title {
        font-size: 18px;
        font-weight: bold;
        margin-bottom: 16px;
        color: #1677ff; /* Màu xanh success */
        text-align: center;
      }
      .info-box {
        background-color: #f9f9f9;
        border: 1px solid #eeeeee;
        border-radius: 6px;
        padding: 16px;
        margin: 20px 0;
      }
      .info-item {
        display: flex;
        justify-content: space-between;
        margin-bottom: 8px;
        font-size: 14px;
      }
      .label {
        color: #888888;
      }
      .value {
        color: #333333;
        font-weight: bold;
      }
      .text {
        font-size: 14px;
        color: #555555;
        line-height: 1.6;
      }
      .footer {
        margin-top: 24px;
        font-size: 12px;
        color: #999999;
        text-align: center;
      }
      .button-wrapper {
        text-align: center;
        margin-top: 24px;
      }
      .button {
        background-color: #1677ff;
        color: #ffffff;
        padding: 10px 24px;
        text-decoration: none;
        border-radius: 4px;
        font-size: 14px;
        font-weight: bold;
      }
    </style>
  </head>

  <body>
    <div class="container">
      <div class="title">🎉 Thanh toán thành công!</div>

      <p class="text">
        Xin chào
        <b>{{name}}</b>,
      </p>

      <p class="text">
        Cảm ơn bạn đã tin dùng dịch vụ của chúng tôi
      </p>

      <div class="info-box">
        <div class="info-item">
          <span class="label">Gói dịch vụ:</span>
          <span class="value">{{subName}}</span>
        </div>
        <div class="info-item">
          <span class="label">Hình thức:</span>
          <span class="value text-capitalize">{{type}}</span>
        </div>

        <div class="info-item">
          <span class="label">Ngày hết hạn:</span>
          <span class="value">{{expiryDate}}</span>
        </div>
      </div>

      <div class="footer">
        ©
        {{year}}
        Management Accounts. All rights reserved.<br />
        Đây là email tự động, vui lòng không phản hồi email này.
      </div>
    </div>
  </body>
</html>